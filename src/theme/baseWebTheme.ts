// themes/customTheme.ts

import { createTheme, lightThemePrimitives } from 'baseui';

// Create a custom theme
export const baseWebTheme = createTheme({
    colors: {
        calendarDayBackgroundSelectedHighlighted: '#FFC94A',
        borderSelected: '#FFC94A',
        buttonPrimaryFill: '#809F80',
        dialogBackground: '#000',
    },
});