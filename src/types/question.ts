export type QuestionType = {
    _id: string;
    questionImage: string;
    answer: string;
    topic: string;
    year: number;
    questionNo: number;
    createdAt: string;
    updatedAt: string;
}
